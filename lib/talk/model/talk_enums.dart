enum TalkPermission {
  UNDETERMINED,
  DENIED,
  GRANTED,
  UNKNOWN,
}

enum TalkConsent {
  OPT_IN,
  OPT_OUT,
  UNKNOWN,
}

enum TalkStatus {
  CONNECTING,
  CONNECTED,
  DISCONNECTED,
  FAILED,
  RECONNECTING,
  RECONNECTED,
  UNKNOWN,
}

enum TalkAudioRoutingOption {
  BLUETOOTH,
  BUILT_IN,
  UNKNOWN,
}
