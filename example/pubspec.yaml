name: zendesk2_example
description: Demonstrates how to use the zendesk2 plugin.
publish_to: 'none' 
version: 1.0.0+0

environment:
  sdk: '>=2.12.0 <3.0.0'
  flutter: ">=1.20.0"

dependencies:
  flutter:
    sdk: flutter

  zendesk2:
    path: ../

  cupertino_icons:
  font_awesome_flutter:
  file_picker:
  image_picker:
  cached_network_image:
  url_launcher:
    
dev_dependencies:
  flutter_test:
    sdk: flutter
    
flutter:
  uses-material-design: true